# Pitch deck (HackMIT 2026)

The 10 slides presented, as HTML slide sections (1920x1080) plus `deck.json` (order, sections, fonts).
Images referenced as `/_blob/<id>` in the slides are the files in `img/`:
cover and close = `arm_wide.jpg`; demo = `detect.jpg`; learned = `matrix_still.jpg` (video `matrix_h264.mp4`);
safety/how = `sim_feed_c.jpg`; everyone = `sim_matrix_c.jpg`; today = `table_wide.jpg`.
Speaker notes are the `<aside>` at the end of each slide.
